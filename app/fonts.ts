import { Playpen_Sans, Ubuntu_Mono, Fira_Sans, Ubuntu } from 'next/font/google';

export const playpenSans = Playpen_Sans({
    subsets: ['latin'],
    weight: ['400', '700'],
});


export const ubuntu = Ubuntu({
    subsets: ['latin'],
    weight: ['400', '700'],
});


export const ubuntuMono = Ubuntu_Mono({
    subsets: ['latin'],
    weight: ['400', '700'],
});